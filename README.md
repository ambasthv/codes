now, i need to  plot charts that show the lines by the width of the bins by ratios (these three ratios), give options to # Lifestages to exclude
exclude_lifestages = [].
Also when you are plotting, plot Early Stage/Emerging Tech on a secondary axis
