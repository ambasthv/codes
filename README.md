i have spotted this line but not exactly you mentioned, should i replace it with you sugesstopm

# load in OW color scheme and plot style
plt.style.use(str(
    CODE_ROOT / 'model_development/utils/resources/ow_style.mplstyle'
))
